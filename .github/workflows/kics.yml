---
name: kics
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: run kics Scan
        uses: checkmarx/kics-github-action@v1.6
        with:
          path: './playbooks/terraform_deploy.yml'
...
