---
name: kics
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: run kics Scan
        uses: checkmarx/kics-github-action@latest
        with:
          path: 'playbooks'
...
